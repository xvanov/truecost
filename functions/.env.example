# TrueCost Python Functions - Local Development Configuration
# Copy this file to .env and customize as needed.
# This file contains NON-SECRET configuration only.

# =============================================================================
# EMULATOR CONFIGURATION
# =============================================================================
# Enable Firebase emulator mode
USE_FIREBASE_EMULATORS=true
FIRESTORE_EMULATOR_HOST=localhost:8081
FUNCTIONS_EMULATOR=true
GCLOUD_PROJECT=collabcanvas-dev

# =============================================================================
# A2A PROTOCOL
# =============================================================================
# Agent-to-Agent communication URL
# Use port 5003 to avoid conflicts with Firebase Functions emulator on port 5001
A2A_BASE_URL=http://127.0.0.1:5003/collabcanvas-dev/us-central1

# =============================================================================
# LLM CONFIGURATION (non-secrets)
# =============================================================================
LLM_MODEL=gpt-4o
LLM_TEMPERATURE=0.1
LOG_LEVEL=INFO

# Pipeline settings
PIPELINE_MAX_RETRIES=2
PIPELINE_PASSING_SCORE=60
MONTE_CARLO_ITERATIONS=10000

# =============================================================================
# SECRETS
# =============================================================================
# IMPORTANT: Secrets should NOT be stored in this file!
#
# For LOCAL DEVELOPMENT:
# Set secrets as environment variables in your shell before running:
#   export OPENAI_API_KEY='sk-proj-...'
#   export SERP_API_KEY='...'
#   export BLS_API_KEY='...'
#
# For PRODUCTION:
# Secrets are automatically loaded from Firebase Secrets Manager.
# To set/update secrets:
#   firebase functions:secrets:set OPENAI_API_KEY
#   firebase functions:secrets:set SERP_API_KEY
#   firebase functions:secrets:set BLS_API_KEY
